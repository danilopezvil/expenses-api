// Deprecated: use expense.void() for logical deletion.
// Hard-delete is still available via IExpenseRepository.delete() if needed by delivery layer.
// Kept for DI backward compatibility — remove when delivery layer is updated.
export {};
